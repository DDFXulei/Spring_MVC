<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC
        "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--设置别名    -->
    <typeAliases>
        <!--name: 实体类所在的包名（不是实体类的包名也可以）        -->
        <package name="com.josework.domain"/>
    </typeAliases>
    <!-- SQL映射文件的位置    -->
    <mappers>
        <!--
        name: 是包名，这个包中所有的mapper.xml，一次都能加载。
        使用 package的要求：
            1. mapper文件的名称必须和dao接口名称完全一样，包括大小写。
            2. mapper文件必须和dao接口在同一目录。

                -->
        <package name="com.josework.dao"/>
    </mappers>

</configuration>